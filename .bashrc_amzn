
## Aliases ##

# brazil-build
if [ $(date +%A) = "Friday" ]
then
  alias bb='brazil-build-rainbow'
else
  alias bb='brazil-build'
fi

start_debug_tunnel() {
  ssh -L $1:127.0.0.1:$1 sstavino.aka.corp.amazon.com -f -N
}

# Path
export PATH=$HOME/.toolbox/bin:$PATH

#!/bin/bash

# Echo the command that was sent to the script

# send the message to emacs-client gptel-send-to-new-session function
log_message "thinking..."
emacsclient -n --eval "(gptel-send-to-new-session \"$1\")"

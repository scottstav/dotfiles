#!/bin/bash
# Toggle voice typing pause/resume via signal (instant)

PID=$(pgrep -f "enhanced-voice-typing.py")

if [ -z "$PID" ]; then
    notify-send -u critical -t 2000 "Voice Typing" "Not running"
    exit 1
fi

kill -SIGUSR1 "$PID"
notify-send -t 500 "Voice Typing" "Toggled"

#!/bin/bash
# Toggle streaming voice typing pause/resume via signal

PID=$(pgrep -f "streaming-voice-typing.py")

if [ -z "$PID" ]; then
    notify-send -u critical -t 2000 "Stream Typing" "Not running — start daemon first"
    exit 1
fi

kill -SIGUSR1 "$PID"

# Stow ignore file
# These paths won't be symlinked to home

# Restore defaults that .stow-local-ignore overrides
\.git
\.gitignore
README\.md
^/CLAUDE\.md

# Repo scripts and metadata — not part of the home directory
bootstrap\.sh
setup\.sh
sync-to-vc\.sh
sync-claude\.sh
packages.*\.txt

# Android stuff is pushed via ADB, not stowed
android

# Python venvs — managed by pip, not stow
\.venv

# Root/etc configs are stowed separately with: sudo stow --target=/ root
root
